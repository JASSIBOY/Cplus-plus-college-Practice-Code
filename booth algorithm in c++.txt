#include<bits/stdc++.h>
using namespace std;
int main()
{
    int A[10],Q[10],A1[10],Q1=0,M[10],M1[10],i,j,d,bit,bits,flag=0,tot;
    int temp,temp1,Q2[100];
    cout<<"1st size of bit"<<endl;
    cin>>bit;
    cout<<"enter the bit for 1st number"<<endl;
    for(i=0;i<bit;i++)
        cin>>Q[i];
    cout<<"2nd size of bit"<<endl;
    cin>>bits;
    cout<<"enter the bit for 2nd number"<<endl;
    for(j=0;j<bits;j++)
        cin>>M[j];
    for(i=0;i<bits;i++)
    {
         A[i]=0;
        A1[i]=0;
    }

    for(i=0;i<bits;i++)
    {
        if(M[i]==0)
            M1[i]=1;
        else
            M1[i]=0;
    }
    temp=1;
    for(i=bit-1;i>0;i--)
    {
        M1[i]=M1[i]+temp;
        if(M1[i]>=2)
        {
            M1[i]=1;
            temp=1;
        }
        else
            temp=0;
    }
          tot=0;
    for(i=0;i<bit;i++)
    {
        if(Q[i]==0)
        {
           tot++;
        }
    }
    if(tot==bit)
    {
        flag=1;
        for(i=0;i<bit;i++)
        {
             A[i]=0;
             Q[i]=0;
        }
    }

    tot=0;
     for(i=0;i<bits;i++)
    {
        if(M[i]==0)
        {
           tot++;
        }
    }
    if(tot==bits)
    {
         flag=1;
        for(i=0;i<bits;i++)
        {
             A[i]=0;
             Q[i]=0;
        }
    }

if(flag==0)
{
  for(d=0;d<bit;d++)
    {
        temp1=0;
    if((Q[bit-1]==1)&&(Q1==0))
    {
        for(i=bit-1;i>=0;i--)
        {
            A[i]=A[i]+M1[i]+temp1;
            if(A[i]>=2)
            {
                A[i]=1;
                temp1=1;
            }
            else
            {
                temp1=0;
            }
        }
        for(i=1;i<bit;i++)
        {
            A1[0]=A[0];
            A1[i]=A[i-1];
        }
        for(i=1;i<bit;i++)
        {
            Q2[0]=A[bit-1];
            Q2[i]=Q[i-1];
            Q1=Q[bit-1];
        }
        for(i=0;i<bit;i++)
              A[i]=A1[i];
        for(i=0;i<bit;i++)
            Q[i]=Q2[i];
    }
    else if((Q[bit-1]==0)&&(Q1==1))
    {
        temp1=0;
       for(i=bit-1;i>=0;i--)
        {
            A[i]=A[i]+M[i]+temp1;
            if(A[i]>=2)
            {
                if(A[i]%2==0)
                {
                    A[i]=0;
                temp1=1;
                }
                else
                {
                     A[i]=1;
                    temp1=1;
                }
            }
            else
            {
                temp1=0;
            }
        }
        for(i=1;i<bit;i++)
        {
            A1[0]=A[0];
            A1[i]=A[i-1];
        }
        for(i=1;i<bit;i++)
        {
            Q2[0]=A[bit-1];
            Q2[i]=Q[i-1];
            Q1=Q[bit-1];
        }
        for(i=0;i<bit;i++)
              A[i]=A1[i];
        for(i=0;i<bit;i++)
            Q[i]=Q2[i];
    }
    else
    {
        for(i=1;i<bit;i++)
        {
            A1[0]=A[0];
            A1[i]=A[i-1];
        }
        for(i=1;i<bit;i++)
        {
            Q2[0]=A[bit-1];
            Q2[i]=Q[i-1];
            Q1=Q[bit-1];
        }
        for(i=0;i<bit;i++)
              A[i]=A1[i];
        for(i=0;i<bit;i++)
            Q[i]=Q2[i];
    }
    }
}

    for(i=0;i<bit;i++)
    {
       cout<<A[i]<<" ";
    }
    for(i=0;i<bit;i++)
    {
       cout<<Q[i]<<" ";
    }

    return 0;
}
