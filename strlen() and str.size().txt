#include<iostream>
#include<string.h>

using namespace std;
int main()
{
    int s,p,q;
    string str;
    char a[100],b[100];
    cin>>a>>b;
  cin>>str;
     s=strlen(a);
     p=strlen(b);
     q=str.size();//it
    cout<<s<<p<<q;

    return 0;
}
